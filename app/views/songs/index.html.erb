<h1>All Songs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Duration</th>
  </tr>
  <% @songs.each do |song| %>
  <tr>
    <td><%= song.name  %></td>
    <td><%= song.duration %></td>
  </tr>
  <% end %>
</table>
<br>
<%= link_to "New Song", new_song_path %>
